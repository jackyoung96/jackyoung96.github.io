---

---

# Experiential Reinforcement Learning (ERL) 리뷰: Rollout의 숨겨진 정보를 활용하자

> **TL;DR**: On-policy RL에서 rollout 과정의 정보 낭비 문제를 지적하고, self-reflection을 통해 exploration 효율을 극적으로 높이는 Experiential Reinforcement Learning (ERL) 패러다임 제안

## 들어가며: 우리는 이미 이걸 하고 있다

이 논문을 읽으면서 가장 먼저 든 생각은 "아, 우리가 실무에서 이미 하고 있는 것을 형식화했구나"였다.

**현실에서 일어나는 일을 생각해보자:**

1. 새로운 모델을 배포한다
2. 실제 환경에서 사용하면서 문제점을 발견한다
3. Prompting이나 agent system으로 self-reflection을 통해 성능을 끌어올린다
4. 이렇게 모인 데이터로 SFT를 하거나 RL을 돌린다
5. 개선된 모델을 다시 배포한다
6. **1-5를 반복한다**

ERL은 바로 이 cycle을 training loop 안에 내재화시킨 것이다. 실무에서 수동으로 하던 "경험 → 반성 → 개선 → 내재화"의 흐름을 RL 학습 과정에 녹여낸 셈이다.

## 문제 제기: Rollout은 비싸고, Reward는 너무 단순하다

On-policy RL을 NLP에 적용할 때 가장 큰 병목이 뭘까?

**Rollout이 가장 오래 걸린다.** 모델이 전체 response를 생성하고, 환경과 상호작용하고, trajectory를 수집하는 과정은 계산적으로 매우 비싸다.

**그런데 그 결과물에서 얻는 피드백은?** 단순한 scalar reward뿐이다.

이게 얼마나 낭비적인지 생각해보자. Reasoning model의 경우 생성된 response에는 **엄청나게 풍부한 정보**가 담겨 있다:
- 어디서 실수했는지
- 어떤 reasoning path를 따랐는지
- 어떤 전략이 작동하지 않았는지

하지만 기존 RLVR(Reinforcement Learning with Verifiable Rewards)는 이 모든 정보를 무시하고 "맞았다/틀렸다"라는 단순한 신호만 사용한다. 모델은 implicit하게 "대체 뭘 잘못했지?"를 추론해야 한다.

## ERL의 핵심: Experience-Reflection-Consolidation Loop

ERL은 이 문제를 정면으로 해결한다. 핵심 아이디어는:

### 1단계: First Attempt
```
x (task) → πθ → y(1) (first attempt) → (f(1), r(1)) (feedback, reward)
```

일단 첫 번째 시도를 한다. 여기까진 기존 RL과 동일하다.

### 2단계: Self-Reflection
```
(x, y(1), f(1), r(1), m) → πθ → Δ (reflection)
```

여기가 핵심이다. 첫 시도가 실패하면, 모델이 **스스로** "뭘 잘못했고 어떻게 고쳐야 하는지" reflection을 생성한다. `m`은 cross-episode memory로, 이전에 효과적이었던 corrective pattern을 저장한다.

### 3단계: Second Attempt (Retry)
```
(x, Δ) → πθ → y(2) → (f(2), r(2))
```

Reflection을 conditioning으로 두 번째 시도를 한다. Reflection이 효과적이라면 성능이 올라간다.

### 4단계: Internalization (핵심!)
```
ℒ_distill(θ) = -𝔼[𝕀(r(2) > 0) log πθ(y(2)|x)]
```

**이게 진짜 중요한 부분이다.** 성공한 두 번째 시도를 **reflection 없이** 원래 input만으로 재현하도록 distillation한다.

왜 이게 중요하냐면:
- **Inference time에 reflection이 필요 없다**: 학습 중에 얻은 insight가 base policy에 내재화된다
- **배포 후 추가 비용이 없다**: Self-reflection을 돌릴 필요 없이 바로 개선된 행동을 한다

## 실험 결과: 어디서 효과가 클까?

| Environment | Qwen3-4B (RLVR → ERL) | Olmo3-7B (RLVR → ERL) |
|------------|----------------------|----------------------|
| Sokoban | 0.06 → 0.87 (+81%) | 0.04 → 0.20 |
| FrozenLake | 0.72 → 0.91 (+27%) | 0.70 → 0.90 |
| HotpotQA | 0.55 → 0.61 (+11%) | 0.51 → 0.57 |

**가장 효과가 큰 곳**: Sokoban, FrozenLake 같은 sparse-reward + unknown dynamics 환경

이유가 명확하다:
1. **환경 규칙을 직접 알려주지 않는다**: 모델이 trial-and-error로 추론해야 함
2. **Long-horizon planning 필요**: 작은 실수가 누적되어 실패로 이어짐
3. **Sparse reward**: 최종 결과에서만 피드백을 받음

이런 조건에서는 "implicit하게 뭘 잘못했는지 추론"하는 게 거의 불가능하다. 하지만 explicit reflection이 있으면 "아, 저기서 왼쪽으로 갔어야 했는데"라는 구체적인 교정이 가능하다.

반면 HotpotQA는 개선폭이 상대적으로 작다. Tool-using이 주된 task라 interaction pattern이 homogeneous하고, reward도 더 dense하기 때문이다.

## 왜 이게 의미있나: 실무적 관점

### 1. Agent System의 미래를 보여준다

현재 우리가 agent를 만들 때:
```
Base Model → Prompting으로 성능 끌어올림 → 데이터 수집 → Fine-tuning → 반복
```

ERL은 이 cycle을 **자동화**한다. Agent가 스스로:
- 경험에서 배우고
- 반성하고
- 내재화한다

### 2. Inference Cost 절감

Self-reflection을 inference time에 하면 비용이 2배다. 하지만 ERL의 internalization step 덕분에 배포된 모델은 reflection 없이 바로 개선된 행동을 한다.

### 3. Exploration Efficiency

기존 RL은 "좋은 trajectory를 우연히 발견"해야 한다. ERL은 실패한 trajectory에서도 **구조화된 개선 방향**을 추출한다. 이게 sample efficiency를 극적으로 높인다.

## 한계와 생각할 점

### Memory가 양날의 검
Cross-episode memory가 잘못된 reflection을 저장하면 오히려 방해가 된다. Olmo3-7B Sokoban에서 no-memory variant가 full ERL을 약간 이겼다. 모델의 self-reflection 능력이 부족하면 memory가 noise를 축적할 수 있다.

### Self-Reflection 능력 의존성
결국 모델이 "좋은 reflection"을 생성할 수 있어야 한다. 이건 base model의 능력에 크게 의존한다.

### Compute Budget
두 번 attempt + reflection 생성 = rollout 비용 증가. 논문에서는 RLVR에 더 많은 rollout을 할당해서 fair comparison을 했지만, 실무에서는 trade-off 고려 필요.

## 마무리

이 논문이 지적하는 핵심 문제는 명확하다:

> **On-policy RL에서 rollout은 가장 비싼 연산인데, 거기서 나오는 풍부한 정보를 scalar reward로 압축해버리는 건 낭비다.**

그리고 해결책도 직관적이다:

> **Generated response에 담긴 정보를 explicit reflection으로 추출하고, 성공한 교정을 base policy에 내재화하자.**

사실 이건 인간이 학습하는 방식과 똑같다. 실수하고, 왜 실수했는지 반성하고, 다음에 더 잘하려고 노력하고, 결국 의식하지 않아도 올바른 행동이 자연스러워진다.

ERL은 이 "경험적 학습"을 LLM training에 formalize한 것이다. Agent system이 점점 복잡해지는 시대에, 이런 방향의 연구가 더 많이 나올 것 같다.

---

## References
- Paper: [Experiential Reinforcement Learning (arXiv:2602.13949)](https://arxiv.org/abs/2602.13949)
- Authors: Taiwei Shi, Sihao Chen, Bowen Jiang, Linxin Song, Longqi Yang, Jieyu Zhao
- Affiliations: USC, Microsoft, UPenn
